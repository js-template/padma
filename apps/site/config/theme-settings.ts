export const activateTheme = "@padma/metajob-ui"

import { getPublicComponents, getPrivateComponents } from "@padma/metajob-ui" // Adjust the path to your theme package

// Export the themeComponentMapping for site-wide usage
export { getPublicComponents, getPrivateComponents }
