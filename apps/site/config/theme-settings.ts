import { blockComponentMapping } from "@padma/metajob-ui" // Adjust the path to your theme package

console.log("blockComponentMapping", blockComponentMapping)
// Export the themeComponentMapping for site-wide usage
export { blockComponentMapping }
