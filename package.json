{
  "name": "padma-monorepo",
  "description": "Padma - The Next.js Framework for Strapi",
  "version": "0.0.4",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "latest"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
