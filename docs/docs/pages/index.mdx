import Image from 'next/image'
import { Card, Cards } from 'nextra-theme-docs'



{<h1 className="mt-10 mb-4 text-center text-[2.5rem] font-bold tracking-tight">Padma Documentation</h1>}


<Cards>
	{[
		{
			name: 'Main Docs',
			url: '/user-docs/get-started/intro/',
	
		},
		{
			name: 'CLI Docs',
			url: '/cli-docs/intro/',
			//image: HTMLCoverImg,
		},
	].map(({ name, url, image }) => (
		<Card key={name} image arrow title={name} href={url}>
			
		</Card>
	))}
</Cards>